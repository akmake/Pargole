/**
 * DWG Converter utility
 * Checks whether a native DWG-to-DXF converter is available on the host.
 * Currently returns false (stub) — replace with actual converter detection if needed.
 */

export async function hasDwgConverter() {
  return false;
}
